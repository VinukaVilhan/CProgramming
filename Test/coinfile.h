// coinfile.h
#ifndef COINFILE_H
#define COINFILE_H

// Validates the given file according to the assignment rules
// Returns 1 if valid, 0 if invalid (prints error messages)
int ValidateFile(const char* filename);

#endif
